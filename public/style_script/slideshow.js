document.addEventListener( 'DOMContentLoaded', function () {
    new Splide( '.splide', {
        type       : 'fade',
        heightRatio: 0.4,
        cover      : true,
        rewind: true,

    } ).mount();
} );

